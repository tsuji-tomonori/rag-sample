# Repository skills

These skills retain the durable engineering controls from `rag-assist`. GPT-5.6 `/goal`
already provides continuation, completion auditing, budget accounting, and blocked-state
handling, so the older dreaming memory, completion JSON, recovery loop, and milestone
orchestrator skills are intentionally omitted.

Run `python tools/validate_skills.py` or `task docs:check` after editing a skill.
